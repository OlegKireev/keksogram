'use strict';

var previewModal = document.querySelector('.img-upload__preview-container');
var effectValue = previewModal.querySelector('.scale__value').value;
var effectLine = previewModal.querySelector('.scale__line');
var effectPin = previewModal.querySelector('.scale__pin');

effectPin.addEventListener('mouseup', function(evt) {
  console.log('Я не знаю как это сделать');
});
